# @palmprint/core

Shared Palmprint types and tiny token helpers used by the React and server
SDKs.

```ts
import type { SecurityLevel, SessionPayload } from "@palmprint/core";
```

Most applications should import `@palmprint/react` or `@palmprint/server`
directly.
